# Example environment variables for MYBookmarks-Backend
# Use this file as a template; copy to .env and update values for your environment.

# Server
PORT=3001

# Database
# DB_CLIENT can be 'sqlite3' (default) or 'mysql2'
DB_CLIENT=sqlite3
# For sqlite, provide a file path (relative to repo or absolute)
DB_FILE=bookmarks.db
# For MySQL, provide DB_HOST, DB_USER, DB_PASSWORD, DB_NAME and set DB_CLIENT=mysql2
DB_HOST=127.0.0.1
DB_USER=root
DB_PASSWORD=
DB_NAME=mybookmarks

# JWT
JWT_SECRET=change-me-to-a-strong-secret
JWT_EXPIRES=7d

# Registration toggle (true/false)
REGISTRATION_ENABLED=true

# Other
# Set to 'false' if you don't want swagger docs exposed in certain envs
SWAGGER_ENABLED=true
SWAGGER_BASE_URL="http://127.0.0.1:3001" # Optional: override the server URL shown in Swagger UI; use a full https:// URL in production

# CORS_ALLOWED_ORIGINS: comma-separated list of allowed origins for CORS (leave empty to allow all origins)
# Examples:
# CORS_ALLOWED_ORIGINS=https://example.com,http://localhost:3000
# CORS_ALLOWED_ORIGINS=  # allow any origin (default when unset)
CORS_ALLOWED_ORIGINS=
